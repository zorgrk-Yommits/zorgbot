# Mistral API
MISTRAL_API_KEY=your_api_key_here

# Redis Cache (optional, for caching)
REDIS_URL=redis://localhost:6379

# Or use in-memory cache (no Redis required)
# Just set enable_cache=False in client init
